# 2026-09-10 학습 정리

## Spring Boot 배포 설정

오늘은 기존 게시판 프로젝트와 별도로 `deployment` 프로젝트를 생성하고, Spring Boot 애플리케이션을 **JAR/WAR 형태로 배포할 수 있도록 설정**했다.

### 1. Gradle WAR 플러그인 추가

`build.gradle`에 `war` 플러그인을 추가했다.

```gradle
plugins {
    id 'java'
    id 'war'
    id 'org.springframework.boot' version '4.1.1'
    id 'io.spring.dependency-management' version '1.1.7'
}
```

WAR(Web Application Archive)는 웹 애플리케이션을 서버에 배포할 때 사용하는 패키징 형식이다.

### 2. Spring Boot 버전 및 프로젝트 버전 설정

Spring Boot 버전을 `4.1.1`로 설정하고 프로젝트 버전을 `1.0.2`로 변경했다.

```gradle
version = '1.0.2'
```

### 3. Tomcat 의존성 설정

외부 Tomcat 환경에 배포할 수 있도록 Tomcat을 `providedRuntime`으로 설정했다.

```gradle
providedRuntime 'org.springframework.boot:spring-boot-starter-tomcat:4.1.1'
```

애플리케이션을 WAR로 만들어 외부 Servlet 컨테이너에서 실행할 때 필요한 설정이다.

---

## SpringBootServletInitializer

외부 Tomcat에서 Spring Boot 애플리케이션을 실행하기 위해 `ServletInitializer`를 추가했다.

```java
public class ServletInitializer extends SpringBootServletInitializer {

    @Override
    protected SpringApplicationBuilder configure(SpringApplicationBuilder application) {
        return application.sources(DemoApplication.class);
    }
}
```

외부 Servlet 컨테이너가 Spring Boot 애플리케이션을 시작할 수 있도록 연결해주는 역할을 한다.

---

## Profile

Spring Profile을 이용하면 실행 환경에 따라 설정을 분리할 수 있다.

```properties
spring.profiles.active=dev
```

`dev` 프로파일을 활성화하면 `application-dev.properties`와 같은 프로파일 전용 설정을 사용할 수 있다.

---

## Profile을 이용한 개발 환경 초기화

`DataInitializer`에 `@Profile("dev")`를 적용하여 개발 환경에서만 초기 데이터를 저장하도록 구성했다.

```java
@Component
@Profile("dev")
public class DataInitializer implements ApplicationRunner {
    // 초기 데이터 저장
}
```

---

## 애플리케이션 빌드

Spring Boot 웹 애플리케이션은 실행 환경에 따라 JAR 또는 WAR로 패키징할 수 있다.

```text
Spring Boot 애플리케이션
        ↓
   빌드 / 패키징
        ↓
 ┌──────┴──────┐
 ↓             ↓
JAR           WAR
 ↓             ↓
직접 실행      외부 Servlet 컨테이너
               ↓
             Tomcat
```

---

## Member API 실습

`MemberController`를 만들고 `GET /members` 회원 목록 조회 API를 추가했다.

```java
@GetMapping("/members")
public List<Member> getMembers() {
    return memberRepository.findAll();
}
```

---

## H2 개발 환경 설정

개발 환경과 테스트 환경을 분리하기 위해 프로파일별 H2 데이터베이스 설정을 추가했다.

### dev

```properties
spring.datasource.url=jdbc:h2:mem:devdb
spring.datasource.username=devu
spring.datasource.password=devp
spring.h2.console.enabled=true
```

### test

```properties
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=testu
spring.datasource.password=testp
```

개발 환경과 테스트 환경에서 서로 다른 H2 데이터베이스를 사용하도록 구성했다.

---

## 오늘 작업한 내용 정리

- `deployment` 프로젝트 생성
- Gradle `war` 플러그인 추가
- Spring Boot `4.1.1` 및 프로젝트 버전 `1.0.2` 설정
- 외부 Tomcat 배포를 위한 `providedRuntime` 설정
- `SpringBootServletInitializer` 구현
- Spring Profile 및 `spring.profiles.active` 학습
- `dev` 프로파일용 `DataInitializer` 구현
- `Member` Entity 및 `MemberRepository` 구성
- `GET /members` 회원 조회 API 구현
- dev/test 프로파일별 H2 데이터베이스 설정
- Spring Boot 웹 애플리케이션의 JAR/WAR 빌드 및 외부 Servlet 컨테이너 배포 구조 학습
